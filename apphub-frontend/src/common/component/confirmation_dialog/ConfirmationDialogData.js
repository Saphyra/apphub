const ConfirmationDialogData = class {
    constructor(id, title, content, choices) {
        this.id = id;
        this.title = title;
        this.content = content;
        this.choices = choices;
    }
}

export default ConfirmationDialogData;