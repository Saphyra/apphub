spring:
  application:
    name: user-authentication
  cloud:
    kubernetes:
      config:
        sources:
          - name: ribbon-config
          - name: common-config
          - name: database-config