spring:
  application:
    name: user

liquibase:
  schema: apphub_user

accessToken:
  cookie:
    expirationDays: 365
  expirationMinutes: 10

registration:
  defaultRoles:
    - NOTEBOOK
    - SKYXPLORE
    - ACCESS

rolesCanBeDisabled:
  - NOTEBOOK
  - SKYXPLORE
  - TRAINING
  - TEST