server:
  port: 8085

deleteAccountBatchCount: 50

aws:
  dynamoDb:
    accessKeyId: dynamodbaccesskeyid
    secretKey: dynamodbsecretkey
    url: http://localhost:8000
    user:
      tableName: apphub-local-user

logging:
  level:
    com:
      github:
        saphyra:
          apphub:
            service:
              user: INFO