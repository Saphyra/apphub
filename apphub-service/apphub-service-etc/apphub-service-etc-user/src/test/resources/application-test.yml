spring:
  datasource:
    username: root
    url: jdbc:h2:mem:test
    driverClassName: org.h2.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

aws:
  dynamoDb:
    user:
      tableName: user-test

registration:
  defaultRoles:
    - TEST