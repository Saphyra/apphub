spring:
  application:
    name: modules

liquibase:
  schema: modules

modules:
  modules:
    accounts:
      - name: account
        url: /web/user/account
        allowedByDefault: true
      - name: admin-panel
        url: /web/admin-panel
        allowedByDefault: false
        roles:
          - ADMIN
    office:
      - name: notebook
        url: /web/notebook
        allowedByDefault: false
        roles:
          - NOTEBOOK