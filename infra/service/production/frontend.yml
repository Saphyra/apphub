apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app: frontend
    type: spring-app
spec:
  replicas: 1
  selector:
    matchLabels:
      name: frontend
  template:
    metadata:
      labels:
        name: frontend
    spec:
      containers:
      - name: frontend
        image: saphyra/apphub-frontend:release
        ports:
        - containerPort: 3000
        imagePullPolicy: Never
        startupProbe:
          httpGet:
            path: /
            port: 3000
          periodSeconds: 5
          failureThreshold: 3600
        livenessProbe:
          httpGet:
            path: /
            port: 3000
          periodSeconds: 5
          failureThreshold: 1
        readinessProbe:
          httpGet:
            path: /
            port: 3000
          periodSeconds: 5
          failureThreshold: 1
        resources:
          limits:
            memory: 200Mi
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: frontend
spec:
  type: ClusterIP
  ports:
  - port: 3000
  selector:
    name: frontend