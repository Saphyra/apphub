apiVersion: apps/v1
kind: Deployment
metadata:
  name: diary
  labels:
    app: diary
    type: spring-app
spec:
  replicas: 1
  selector:
    matchLabels:
      name: diary
  template:
    metadata:
      labels:
        serviceType: spring-app
        name: diary
    spec:
      containers:
        - name: diary
          image: saphyra/apphub-service-diary:release
          ports:
            - containerPort: 8080
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              path: /platform/health
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /platform/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          resources:
            limits:
              memory: 500Mi
---
apiVersion: v1
kind: Service
metadata:
  name: diary
  labels:
    app: diary
spec:
  type: ClusterIP
  ports:
    - port: 8080
  selector:
    name: diary