apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-panel
  labels:
    app: admin-panel
    type: spring-app
spec:
  replicas: 1
  selector:
    matchLabels:
      name: admin-panel
  template:
    metadata:
      labels:
        serviceType: spring-app
        name: admin-panel
    spec:
      containers:
        - name: admin-panel
          image: saphyra/apphub-service-admin_panel:latest
          ports:
            - containerPort: 8080
          imagePullPolicy: Never
          livenessProbe:
            httpGet:
              path: /platform/health
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /platform/health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 1
            failureThreshold: 15
          resources:
            limits:
              memory: 500Mi
---
apiVersion: v1
kind: Service
metadata:
  name: admin-panel
  labels:
    app: admin-panel
spec:
  type: ClusterIP
  ports:
    - port: 8080
  selector:
    name: admin-panel