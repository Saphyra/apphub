apiVersion: apps/v1
kind: Deployment
metadata:
  name: skyxplore-data
  labels:
    app: skyxplore-data
    type: spring-app
spec:
  replicas: 1
  selector:
    matchLabels:
      name: skyxplore-data
  template:
    metadata:
      labels:
        serviceType: spring-app
        name: skyxplore-data
    spec:
      containers:
        - name: skyxplore-data
          image: saphyra/apphub-service-skyxplore-data:latest
          ports:
            - containerPort: 8080
          imagePullPolicy: Never
          livenessProbe:
            httpGet:
              path: /platform/health
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /platform/health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 1
            failureThreshold: 15
          resources:
            limits:
              memory: 1000Mi
---
apiVersion: v1
kind: Service
metadata:
  name: skyxplore-data
  labels:
    app: skyxplore-data
spec:
  type: ClusterIP
  ports:
    - port: 8080
  selector:
    name: skyxplore-data