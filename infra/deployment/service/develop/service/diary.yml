apiVersion: apps/v1
kind: Deployment
metadata:
  name: diary
  labels:
    app: diary
    type: spring-app
spec:
  replicas: 1
  selector:
    matchLabels:
      name: diary
  template:
    metadata:
      labels:
        serviceType: spring-app
        name: diary
    spec:
      containers:
        - name: diary
          image: saphyra/apphub-service-diary:latest
          ports:
            - containerPort: 8080
          imagePullPolicy: Never
          livenessProbe:
            httpGet:
              path: /platform/health
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /platform/health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 1
            failureThreshold: 15
          resources:
            limits:
              memory: 350Mi
---
apiVersion: v1
kind: Service
metadata:
  name: diary
  labels:
    app: diary
spec:
  type: ClusterIP
  ports:
    - port: 8080
  selector:
    name: diary