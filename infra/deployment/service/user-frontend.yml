apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-frontend
  labels:
    app: user-frontend
    type: spring-app
spec:
  replicas: 1
  selector:
    matchLabels:
      name: user-frontend
  template:
    metadata:
      labels:
        serviceType: spring-app
        name: user-frontend
    spec:
      containers:
        - name: user-frontend
          image: saphyra/apphub-service-user-frontend:0.0.1-SNAPSHOT
          ports:
            - containerPort: 8080
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              path: /internal/health
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /internal/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          resources:
            limits:
              memory: 250Mi
---
apiVersion: v1
kind: Service
metadata:
  name: user-frontend
  labels:
    app: user-frontend
spec:
  type: ClusterIP
  ports:
    - port: 8080
  selector:
    name: user-frontend