TERRAFORMING
    TODO After implementing surface terraforming, a check should be added to construct new building service, what blocks the new construction, if the terraforming is already in progress.

DISPLAY QUEUE
    TODO After implementing display queue feature, it should be updated with the new items automatically. Construct new building service and upgrade building service should send a WS event about the new item in the queue.
    TODO After implementing display queue feature, it should be updated with the new items automatically. Cancel construction service should send a WS event about item was removed from the queue
    TODO Send ws events about surface changes when cancelling construction / terraformation via queue item

TODO Send ws events about resource changes
