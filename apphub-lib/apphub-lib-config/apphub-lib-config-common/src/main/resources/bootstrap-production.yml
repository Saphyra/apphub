spring:
  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://${postgreSqlHost}:5432/apphub_production?ApplicationName=${spring.application.name}

environment: production

authorization:
  issuer: apphub-prod